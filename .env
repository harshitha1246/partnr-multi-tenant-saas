# Base URL of your backend API (for API calls)
# Use localhost when accessing from your browser
VITE_API_URL=http://localhost:5000/api

# Port for Vite dev server
VITE_PORT=5173

# Host for Vite dev server (allows access from Docker container or network)
VITE_HOST=0.0.0.0
